import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#000',
    },

    loading: {
        flex: 1,
        flexBasis: '100%',
        height: '100%',
        justifyContent: 'center',
        alignContent: 'center',
    },

    loadingText: {
        textAlign: 'center',
        color: '#fff',
    },

    list: {
        gap: 5,
        backgroundColor: '#fac',
    },
})
