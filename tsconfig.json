{
    "extends": "expo/tsconfig.base",
    "compilerOptions": {
        "strict": true,
        "baseUrl": ".",
        "paths": {
            // This needs to be mirrored in babel.config.js
            "components/*": ["src/components/*"],
            "src/*": ["src/*"]
        }
    }
}
